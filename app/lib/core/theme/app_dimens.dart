/// Constantes de layout (espaçamentos, durações) extraídas dos mockups.
/// Centralizadas para manter ritmo visual consistente entre os componentes.
abstract final class AppGaps {
  /// Espaço vertical entre as seções da Home.
  static const section = 13.0;
  static const screenH = 17.0;
}

abstract final class AppDurations {
  static const themeSwitch = Duration(milliseconds: 240);
}
